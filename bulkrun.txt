# Compile all design*.cpp files
for f in design*.cpp; do 
    clang++ -O2 -std=c++20 "$f" -o "${f%.cpp}"; 
done

# Run all benchmarks (excluding source files and headers)
for f in design*; do 
    if [[ -x "$f" && ! "$f" == *.cpp && ! "$f" == *.h ]]; then 
        ./"$f"; 
    fi 
done